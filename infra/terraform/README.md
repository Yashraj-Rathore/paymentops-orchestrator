# Terraform Scaffolding

This directory is reserved for the AWS staging deployment. The target shape is ECS on Fargate behind an Application Load Balancer, with secrets management, logging, and environment-specific variables.

The foundation milestone commits placeholders only. Real resources should be added once the local service contracts are stable.
